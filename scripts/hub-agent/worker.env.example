# Worker defaults — VPS: /opt/hub-agent/config/worker.env
WORKER_ID=contabo-whatsapp-1
POLL_INTERVAL_MS=5000
HUB_AGENT_WORKSPACES_ROOT=/opt/hub-agent/workspaces
HUB_AGENT_LOG_DIR=/opt/hub-agent/logs
HUB_AGENT_REPO_ROOT=/opt/hub-agent/repo
# Require at least one MCP tool call per successful task (Fase 3C validation)
HUB_AGENT_REQUIRE_MCP=1
